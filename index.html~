---
layout: homepage
---
    {% for post in site.posts %}
			<div class="post-preview">
				<h2>
				  <a class="post-link" href="{{ post.url | prepend: site.baseurl }}">{{ post.title }}</a>
				</h2>
				<p>{{ post.shortinfo}}</p>
				<p><a style="color:#ee9285;" href="{{ post.url | prepend: site.baseurl }}">Читати повністю </a>	</p>	
<p class="post-meta">Опубліковано: {{ зщіе.datestring }}</p>	
			</div>
<hr></hr>
    {% endfor %}

</div>
